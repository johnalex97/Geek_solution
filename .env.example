# Public Vite build-time configuration. Never put private secrets in VITE_* variables.
# Create this browser-safe access key at https://web3forms.com/.
# Compose reads .env and passes this value as a Docker build argument.
VITE_WEB3FORMS_ACCESS_KEY=your-web3forms-access-key
