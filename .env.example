# Public Vite build-time configuration. Never put secrets in VITE_* variables.
# Compose reads .env and passes this value as a Docker build argument.
VITE_CONTACT_FORM_ENDPOINT=https://formspree.io/f/tu-endpoint
